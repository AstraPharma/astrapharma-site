# Normalise line endings to LF in the repository, so files behave the same
# on Windows here and on the Linux machines that build and serve the site.
* text=auto eol=lf

# Binary assets must never be line-ending converted.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.woff2 binary
